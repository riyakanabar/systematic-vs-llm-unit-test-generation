from get_variants import get_variation_algorithms

#STep1 run unit tests on input - optimal algorithm
#store the results - no of pieces
#generate variants
#run unit tests on each variant
#compare the results with input's results - pieces
#output the first variant which passes all tests
algorithms = get_variation_algorithms()
